## Dependency Watch (2026-06-15)

Generated by dep-watcher. Covers production dependencies only (`--omit=dev`).
Security audit results reflect `npm audit --omit=dev` at time of run.

---

### `package.json` (root — frontend / build tooling)

#### Security Advisories
None. `npm audit --omit=dev` reports 0 vulnerabilities across 7 production dependencies.

#### Major Version Bumps Available
None.

#### Minor / Patch Bumps Available (batch in monthly sweep)

| Package | Installed range | Wanted (in-range) | Latest |
|---|---|---|---|
| `@sentry/browser` | `^10.53.1` | `10.58.0` | `10.58.0` |

> `@sentry/browser` is within the `^10` semver range; running `npm update` at the root will pull `10.58.0`. No breaking changes expected.

---

### `lambda/package.json` (Lambda — invite handler)

#### Security Advisories
None. `npm audit --omit=dev` reports 0 vulnerabilities across 104 production dependencies.

#### Major Version Bumps Available

| Package | Installed range | Current latest-in-range | New major available | Risk |
|---|---|---|---|---|
| `@octokit/rest` | `^21.0.0` | `21.1.1` | **22.0.1** | Review breaking changes before upgrading — Octokit v22 drops Node 18 support and revises several request-option defaults. Pin to `^21` until Lambda runtime is confirmed on Node 20+. |
| `@sentry/aws-serverless` | `^9.0.0` | `9.47.1` | **10.58.0** | Sentry SDK v10 removes several deprecated APIs and changes the default DSN behaviour. Review [Sentry migration guide](https://docs.sentry.io/platforms/javascript/migration/v9-to-v10/) before upgrading. Coordinate with the root `@sentry/browser` upgrade so SDK versions stay in sync. |

#### Minor / Patch Bumps Available (batch in monthly sweep)

| Package | Installed range | Wanted (in-range) | Latest in-range |
|---|---|---|---|
| `@octokit/rest` | `^21.0.0` | `21.1.1` | `21.1.1` |
| `@sentry/aws-serverless` | `^9.0.0` | `9.47.1` | `9.47.1` |

> Run `npm update --prefix lambda` to pull the latest in-range versions (`21.1.1` / `9.47.1`) with no breaking-change risk.

---

### Summary

| Severity | Count | Action |
|---|---|---|
| CRITICAL / HIGH security advisory | 0 | — |
| Major version bump available | 2 | Review breaking changes; see notes above |
| Minor / patch bump available | 3 | Batch in next monthly sweep — `npm update` + `npm update --prefix lambda` |

No immediate security action required.
