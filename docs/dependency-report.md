## Dependency Watch (2026-06-08)

> Generated by dep-watcher. Covers production dependencies only (`--omit=dev`).
> No security vulnerabilities found in either manifest.

---

### `package.json` (root — portal frontend)

**Security advisories:** none

**Major version bumps:** none

**Minor / patch bumps** _(low priority — batch in monthly sweep)_

| Package | Current range | Wanted (in-range) | Latest |
|---|---|---|---|
| `@sentry/browser` | `^10.53.1` | `10.56.0` | `10.56.0` |

---

### `lambda/package.json` (jaetill-portal-invite Lambda)

**Security advisories:** none

**Major version bumps** _(note — review breaking changes before upgrading)_

| Package | Current range | Wanted (in-range) | Latest | Risk notes |
|---|---|---|---|---|
| `@octokit/rest` | `^21.0.0` | `21.1.1` | `22.0.1` | v22 is a major bump; review Octokit changelog for breaking API changes before upgrading. In-range `21.1.1` is safe to apply now. |
| `@sentry/aws-serverless` | `^9.0.0` | `9.47.1` | `10.56.0` | v10 is a major bump; Sentry SDK v10 dropped several legacy APIs and changed envelope handling. In-range `9.47.1` is safe to apply now. |

**Minor / patch bumps:** none beyond the in-range updates noted above

---

### Summary

| Severity | Count | Action |
|---|---|---|
| CRITICAL / HIGH advisory | 0 | — |
| Major version available | 2 (`lambda/`) | Manual review required before upgrading past current semver range |
| Minor / patch available | 1 (root) | Batch in monthly sweep |

To apply the safe in-range updates:
```bash
# root
npm update @sentry/browser

# lambda
npm update --prefix lambda @octokit/rest @sentry/aws-serverless
```
