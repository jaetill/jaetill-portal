{
  "name": "jaetill-portal-lambdas",
  "version": "1.0.0",
  "private": true,
  "description": "Shared dependencies for jaetill-portal Lambda functions. Each function is bundled with its handler + lib/ + node_modules into a deployment zip.",
  "dependencies": {
    "@octokit/rest": "^21.0.0",
    "@sentry/aws-serverless": "^9.0.0"
  }
}
